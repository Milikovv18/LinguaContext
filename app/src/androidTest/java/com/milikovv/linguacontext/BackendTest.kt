package com.milikovv.linguacontext

import org.junit.Test

class BackendTest {
    @Test
    fun testScan() {
        // Loading test Bitmap
        //val context = InstrumentationRegistry.getInstrumentation().context
        //val drawable = context.resources.getDrawable(com.milikovv.linguacontext.test.R.drawable.book_screenshot, null)
        //val bitmap = (drawable as BitmapDrawable).bitmap

        // Initializing repo
        //ReaderRepo().data = Snapshot(listOf(), bitmap)

        // Scanning
        //Scanner.scan()
    }
}
